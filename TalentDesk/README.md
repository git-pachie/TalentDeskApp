# ClientRegistrationApp

Simple SwiftUI iOS app for registering a client with:

- Name
- Last Name
- Age
- Mobile
- Email

## Open in Xcode

1. Run `xcodegen generate` in this folder if the project file needs to be regenerated.
2. Open `ClientRegistrationApp.xcodeproj` in Xcode.
